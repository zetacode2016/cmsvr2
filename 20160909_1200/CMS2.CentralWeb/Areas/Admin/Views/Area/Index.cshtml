@using GridMvc.Html
@model List<CMS2.Entities.RevenueUnit>
<br />
<h4>Area</h4>

@{
    if (Model != null)
    {
        @Html.Grid(Model).Named("gridArea").Columns(columns =>
        {
            columns.Add(m => m.RevenueUnitName).Titled("Area").Sortable(true).Filterable(true);
            columns.Add(m => m.Cluster.ClusterName).Titled("Cluster").Sortable(true).Filterable(true);
            columns.Add(m => m.City.CityName).Titled("City").Sortable(true).Filterable(true);
            columns.Add().Encoded(false).Sanitized(false).RenderValueAs(m => @<a href="Area/Edit?id=@m.RevenueUnitId">Edit</a>);
            columns.Add().Encoded(false).Sanitized(false).RenderValueAs(m => @<a href="Area/Delete?id=@m.RevenueUnitId">Delete</a>);
        }).WithPaging(10)
    }
}
<br/>
@Html.ActionLink("Add Area", "Add", null, new { @class = "btn btn-default" })